# 2026-09-02

## 个人博客网站（免费方案）搭建
- 在 `C:\Users\Administrator\Desktop\csdnme` 搭建了纯静态个人博客，技术栈：Node.js 脚本 + Markdown 源文件 + GitHub Pages 免费托管。
- 文件结构：
  - `config.js` 站点配置（标题/作者/端口/每页文章数）
  - `build.js` 构建脚本：读取 `src/posts/*.md`（含 frontmatter），用 marked 编译为 `public/` 静态 HTML，生成首页列表+分页，复制 `src/assets`。
  - `serve.js` 本地服务脚本（仅用 Node 内置模块，端口可 `PORT=` 覆盖），带 404 处理与 index 兜底。
  - `deploy.js` 自动化部署：先 build，再用 gh-pages 推送到 `gh-pages` 分支；无 git remote 时安全退出并提示。
  - `start.bat` / `deploy.bat` Windows 一键脚本；`README.md` 含完整部署说明；`.gitignore` 忽略 node_modules 与 public。
  - `src/posts/welcome.md` 示例文章；`src/templates/{index,post}.html`；`src/assets/style.css` 暗色主题。
- 实测：`npm install` 成功，`npm run build` 生成 public，`serve` 各路由 200/404 正确，`deploy` 在无 remote 时优雅退出。
- 依赖：marked ^12, gh-pages ^6（已装入项目 node_modules）。
- 部署目标仓库：github.com/ljjqw/blog，分支 gh-pages（已验证 raw 分支含 index.html；但用户需在 GitHub Settings→Pages 启用并选 gh-pages 分支站点才会 200，且 URL 必须带 /blog/）。
- 注意：github.io 在中国大陆常被墙/很慢，用户可能打不开；备选免费托管 Cloudflare Pages / Netlify / Gitee Pages。

## 扩展：三页面 + 后台编辑服务
- 用户要求：首页介绍 / CSDN 风格博客列表 / 联系方式 / 可在线编辑文章的后台。
- `build.js` 重写为生成 4 类页面：index.html(首页:关于我+最新文章)、blog.html(CSDN卡片列表)、contact.html、posts/<slug>.html；导出 getPosts/parseFrontmatter/slugify 供 admin 复用；构建开头 rmSync 清空 public 避免残留。
- `config.js` 新增 about(支持MD)、contacts(数组)、adminPort。
- `admin.js` 后台服务（默认 4000）：GET /admin 提供纯前端编辑 UI；JSON API 增删改 /api/posts[/:slug]；保存写回 src/posts/*.md 并自动 rebuild；同时托管 public 静态站点（可用 /blog 简写）。`package.json` 增加 `npm run admin`；新增 admin.bat。
- serve.js 增加无扩展名补 .html 兜底（/blog→/blog.html）。
- 实测：build 生成 4 页面正确；serve 全路由 200；admin 新建(中文slug保留)/更新/删除(博客列表同步移除) 均通过；删除在沙箱受 safe-delete 拦截时以“文件已不在原路径”判成功。
- 沙箱注意：WorkBuddy 的 safe-delete 包装会拦截 fs.unlinkSync/rmSync（移动到回收站并报错），属环境限制，用户本机无此问题；删除逻辑已做兼容。
- 新增示例文 src/posts/markdown-guide.md（演示 CSDN 卡片效果）。

## 重构：改为 Nodify/Starlight 风格文档框架 + 后台导入/层级编辑
- 用户要求：仿 miroiu.github.io/nodify 文档站框架；后台可导入 .md 并编辑“关系层级”。
- 架构改为**文档树框架**：源目录 `src/posts` → 重命名为 `src/docs`；每篇 md 支持 frontmatter `parent`(父slug)/`order`(排序)/`group`(分组)；构建出嵌套树。
- `build.js` 重写：readDocs→buildTree(含环检测 wouldCycle)→flatten；生成 `public/index.html`(落地页:站点介绍+目录卡片) 与 `public/docs/<slug>.html`；侧边栏 renderSidebar 用 `<details>` 可折叠树(祖先自动 open)；renderMarkdown 给 h2/h3 加 id 并产出右侧 TOC(renderToc)；renderPrevNext 按 flatten 顺序生成上/下页。`marked` 渲染。
- 新增 `src/templates/doc.html`(文档页)、`src/templates/index.html`(落地页)；删除旧 blog/contact/post 模板。
- `src/assets/style.css` 重写为亮/暗双主题(CSS 变量, [data-theme])，含顶栏/侧边栏树/TOC/上下面板/响应式；`src/assets/app.js` 新增主题切换(localStorage)、Ctrl+K 聚焦搜索(过滤树)、TOC 平滑滚动+IntersectionObserver 高亮。
- `admin.js` 升级：新增 `POST /api/import`({files:[{name,content}]}，批量导入 md，保留原 frontmatter 的 parent/order，slug 冲突加后缀)；`POST /api/hierarchy`({updates:[{slug,parent,order}]} 批量改层级并重建)；编辑器新增 父级/排序 字段；后台 UI 加“层级结构”标签页(导入区+层级表格含完整路径)。
- 实测：build 生成 7 篇示例文档 + 落地页；serve 全路由 200、缺失 404；admin 导入(中文 title→中文slug)与层级保存(把 home 移入 guide 后侧边栏同步)均通过；清理测试残留后 public 干净。
- config.js 新增 `github`(顶栏链接)。README 更新为文档框架说明。

## 落地页改为 Nodify 风格 Showcase（暗色为主）
- 用户要求：落地页仿 miroiu.github.io/nodify 主页（#showcase），有主介绍 + 技术介绍框架，暗色为主色调。
- `src/templates/index.html` 重写为 showcase 落地页：顶部导航(brand/文档/GitHub/主题切换) + Hero(标题+简介+双CTA) + 核心特性网格 + 文档导航卡片 + 「它是如何工作的」三步 + 页脚。不再用侧边栏树（树保留在 docs 阅读页）。
- `config.js` 新增 `features` 数组（6 张特性卡：层级树/搜索/主题/目录/纯静态/后台），可在配置自定义。
- `style.css` 追加 showcase 暗色样式：hero 径向渐变背景、badge、feature-grid、show-card、steps 流程，默认 [data-theme="dark"]。
- `build.js` 渲染 `features_html` 并传给 index；`section_cards` 卡片类由 `section-card` 改为 `show-card` 以匹配新样式。
- 实测：build 生成 landing 含 6 张 feature-card + 4 张 show-card，data-theme="dark" 默认，docs 阅读页(树/搜索/TOC)仍 200 正常。

## 部署改为 Cloudflare Pages（替代 GitHub Pages，国内可直连）
- 用户要求：用 Cloudflare 实现部署。github.io 在国内常被墙，Cloudflare Pages 可直连且免费、serves at 根路径（无 /blog/ 子路径问题，构建已用相对 `../` 路径天然兼容）。
- 新增 `deploy-cf.js`：先 build，再用 Wrangler 把 public/ 上传到 Cloudflare Pages；项目名取 package.json 的 name（可用 CF_PROJECT_NAME 覆盖，自动小写/连字符化）；无 CLOUDFLARE_API_TOKEN+ACCOUNT_ID 时提示可 `npx wrangler login` 或设环境变量；部署失败时按是否 token 模式给出对应指引；不卡死。
- 新增 `deploy-cf.bat`(Win一键)、`wrangler.toml`(name/pages_build_output_dir=public)、`.github/workflows/deploy-cloudflare.yml`(push main→wrangler-action 部署，用 secrets CLOUDFLARE_API_TOKEN/ACCOUNT_ID，vars CF_PROJECT_NAME)。
- `package.json` 增 `deploy:cf` 脚本、更新 keywords/description；`config.js` 特性卡“纯静态零成本”文案改为 Cloudflare Pages；`.gitignore` 加 `.wrangler/`；README 重写部署章节（Cloudflare 三种方式为主，GitHub Pages 为备选）。
- 实测 `node deploy-cf.js`（无凭证）：先 build 成功→wrangler 经 npx 自动下载→非交互环境报错→脚本捕获并打印指引并 exit 1，无卡死。
